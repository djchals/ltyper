HOW TO INSTALL Ltyper
=====================

    This program depends on autotools, libncurses5 and libjson-c3, before installing
it we must first resolve these dependencies. 

    When dependencies are installed the shell commands should configure, build, and install this package.
    	./autogen.sh
	./configure
	make 
	sudo make install

 
